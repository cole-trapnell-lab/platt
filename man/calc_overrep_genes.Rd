% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deg.R
\name{calc_overrep_genes}
\alias{calc_overrep_genes}
\title{Calculate Overrepresented Genes in Pathways}
\usage{
calc_overrep_genes(
  degs_for_cell_state,
  pathway_type,
  pathway_df,
  gene_universe = NULL,
  q_val_thresh = 0.05
)
}
\arguments{
\item{degs_for_cell_state}{A data frame containing differential expression results
for a specific cell state. It must include columns \code{interpretation_simple}
(with values "Up", "Down", or "Maintained") and \code{gene_short_name}.}

\item{pathway_type}{A character string specifying the type of pathway analysis to perform.
(Currently unused in the function but included for potential future use.)}

\item{pathway_df}{A data frame containing pathway information. It must include columns
\code{gene_short_name} (gene names) and \code{gs_name} (pathway names).}

\item{gene_universe}{A character vector of all possible genes to consider in the analysis.
If \code{NULL}, the universe is derived from the input data (\code{degs_for_cell_state}).}

\item{q_val_thresh}{A numeric value specifying the q-value threshold for significance.
Default is 0.05.}
}
\value{
A data frame containing the results of the overrepresentation analysis (ORA),
including the pathways and their significance. The results are filtered to include
only main pathways for "Up" and "Down" genes.
}
\description{
This function identifies overrepresented genes in pathways for a given cell state
based on differential expression analysis. It uses the \code{fgsea} package to perform
overrepresentation analysis (ORA) and collapses pathways to main pathways for clarity.
}
\details{
The function splits the input pathways into gene sets and performs ORA separately
for "Up" and "Down" genes. It uses the \code{fgsea::fora} function for ORA and
\code{fgsea::collapsePathwaysORA} to collapse pathways to main pathways. The results
are combined and returned as a single data frame.
}
