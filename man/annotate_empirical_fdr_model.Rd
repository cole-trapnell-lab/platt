% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/empirical_fdr_model.R
\name{annotate_empirical_fdr_model}
\alias{annotate_empirical_fdr_model}
\title{Annotate a DEG table with per-gene empirical p and BH FDR.}
\usage{
annotate_empirical_fdr_model(
  model,
  deg_tbl,
  log_ratio = NULL,
  detection = NULL,
  support = NULL,
  counts = NULL,
  arm_cells = NULL,
  min_pseudobulks = .EFDR_MIN_PB,
  gates = .EFDR_DEFAULT_GATES,
  min_replicate_embryos = .EFDR_MIN_REP_EMB,
  group_col = "cell_group"
)
}
\arguments{
\item{model}{An \code{efdr_model} from \code{\link[=train_efdr_model]{train_efdr_model()}}.}

\item{deg_tbl}{DEG table with \code{gene_short_name}, \code{cell_group},
\code{log_mean_expression}, and either \code{z} or \code{perturb_to_ctrl_shrunken_lfc} +
\code{perturb_to_ctrl_shrunken_lfc_se}.}

\item{log_ratio}{Deprecated / ignored (the tail is conditioned on the actual
per-cell-type arm cell count, not a single global sampling ratio).}

\item{detection}{Deprecated / unused (the \%embryos covariate is retired).}

\item{support}{Per-cell-type embryo counts (\code{cell_group}, \code{n_pert_pb}) for the
<2-pseudobulk gate.}

\item{counts}{Per-(gene, cell_type) arm UMI totals (\code{id}/\code{gene_short_name},
\code{cell_group}, \code{K_ctrl}, \code{K_pert}) for the gene-absent and control-absent gates.}

\item{arm_cells}{Per-cell-type arm CELL counts (data.frame with \code{cell_group},
\code{n_ctrl_cells}, \code{n_pert_cells}). Supplies the arm-size covariate the tail is
conditioned on. Genes in cell types with no match fall back to ashr only.}

\item{gates}{Character vector of the uncallability gates to APPLY, from
\code{.EFDR_GATES}. Defaults to \code{.EFDR_DEFAULT_GATES} = \code{too_few_embryos},
\code{not_replicated}, \code{too_few_cells}. The previous policy's other five gates
(\code{not_detected_anywhere}, \code{gain_from_zero}, \code{gain_no_counts},
\code{loss_from_trace}, and \code{flat_cell_floor}, the retired per-experiment cell
floor K) are off by default because each is subsumed; pass them explicitly
to reproduce that policy for back-comparison. Gates not listed are still
computed and counted in the \code{efdr_stats} attribute -- they just stop pinning
\code{empirical_p} to 1, so those calls fall back to the two statistical floors
(\code{max(p_ashr, p_tail)}). Unknown names error rather than silently no-op.}

\item{min_replicate_embryos}{Distinct embryos that must detect the gene in at
least one arm for the \code{not_replicated} gate to pass it (default
\code{.EFDR_MIN_REP_EMB} = 2). Needs \code{counts} to carry \code{n_emb_ctrl} / \code{n_emb_pert}
(produced by \code{.efdr_arm_counts}); without them nothing is gated on this axis.}

\item{group_col}{Column to BH-adjust within (default \code{cell_group}).}
}
\value{
\code{deg_tbl} with added \code{empirical_p} and \code{empirical_fdr}.
}
\description{
Annotate a DEG table with per-gene empirical p and BH FDR.
}
\details{
The tail null is conditioned on \strong{expression x thin-arm cell count}
(not \%embryos / global ratio), so a real depletion (huge |z|, deep arm) is
kept while a thin-arm sampling artifact of the same |z| is demoted. Each call
is matched on \code{min(n_ctrl_cells, n_pert_cells)} (the thinner arm) and the sign
of the effect \strong{in that thin arm}, which generalizes to perturbation-heavy
designs (where the control arm is the thin one). \code{empirical_p = max(p_ashr, p_tail)}: the empirical step may only \emph{demote} a call ashr made.
}
