% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/empirical_fdr_model.R
\name{train_efdr_model}
\alias{train_efdr_model}
\title{Train the empirical-FDR null model from control-split null draws.}
\usage{
train_efdr_model(
  null,
  detection = NULL,
  taus = .EFDR_TAUS,
  egrid = .EFDR_EGRID,
  lcgrid = .EFDR_LCGRID,
  we = 0.75,
  wc = 0.4,
  min_n = 40L,
  max_we = 2.5
)
}
\arguments{
\item{null}{Output of \code{\link[=build_empirical_null]{build_empirical_null()}} (gene_short_name, cell_group,
log_mean_expression, z, n_pert_cells).}

\item{detection}{Unused (kept for back-compat with callers).}

\item{taus}{Quantile levels (tail-concentrated) the surface is evaluated at.}

\item{egrid, lcgrid}{Expression and log10(thin-arm cells + 1) grids evaluated on.}

\item{we, wc}{Half-widths of the local window (expression, log10-cells) the tail
quantiles are estimated over; widened adaptively until \code{min_n} draws are in.}

\item{min_n}{Minimum null draws required in a window before its quantiles are used.}
}
\value{
An \code{efdr_model}: per-direction 2-D tail surfaces on (egrid x lcgrid).
}
\description{
Models the null upper tail of |z| as a LOCAL-EMPIRICAL surface over \strong{expression
and the thin-arm cell count}, per direction. Each grid cell's tail quantiles are
estimated from the null draws in a local window around it (widened adaptively until
\code{min_n} draws are in; still-empty cells filled from the nearest evaluated cell;
quantiles made monotone in tau by cummax). The two axes:
\itemize{
\item \strong{expression}: the dispersion mis-pricing that inflates |z| worsens as
expression -> 0, so the tail stays heavy in the sparse extreme-low region
rather than drooping back toward zero.
\item \strong{thin-arm cell count}: a thin arm hits exact/near-zero counts which, times
the trend-dispersion-shrunk SE, manufactures large |z| under no real effect.
This is the axis that separates a real depletion (huge |z|, deep arm) from a
thin-arm sampling artifact -- the previous \%embryos/log-ratio surface could
not, because it never conditioned on the actual arm size.
A local-empirical estimate is used rather than a global shape-constrained smooth
(an earlier scam \code{bs="mpd"} surface), which under-fit the low-expression/thin-arm
corner by borrowing from lighter, better-sampled neighbours.
Draws are POOLED across the sampling grid: the grid exists only to span the
arm-size axis, which is then read directly off \code{n_pert_cells}. Direction is kept
separate. The thin arm in a control-split null is always the pseudo-KO
("NULLPERT") minority arm, so its cell count (\code{n_pert_cells}) is the covariate;
\code{dn} = the gene is lower in that thin arm (z<0), \code{up} = higher.
}
}
