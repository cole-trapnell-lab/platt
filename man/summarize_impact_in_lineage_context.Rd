% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/narrative.R
\name{summarize_impact_in_lineage_context}
\alias{summarize_impact_in_lineage_context}
\title{Summarize Lineage-Wide Perturbation Impact}
\usage{
summarize_impact_in_lineage_context(
  perturbation_description = NULL,
  target_gene_expression,
  dact_results,
  degs,
  ref_expression,
  ontologies,
  combined_psg,
  ai_notes_path = NULL,
  sig_p_val_thresh = 0.05,
  genes_of_interest = NULL,
  empirical_p_thresh = 1,
  llm_fun = NULL,
  max_lineage_depth = Inf,
  cell_types = NULL,
  primary_impact_summary = "",
  excluded_cell_types = NULL,
  abundance_phenotypes = NULL,
  fitness_phenotypes = NULL,
  identity_phenotypes = NULL,
  dact_expectations = NULL,
  effect_type = NULL,
  pre_cited_gene_claims = NULL,
  verbose = FALSE,
  sig_pathways_by_cell_type = NULL,
  sig_pathways_path = NULL,
  drop_llm_columns = FALSE,
  ...
)
}
\arguments{
\item{perturbation_description}{Optional free-text description of the
perturbation, used only to build context text for \code{llm_fun}.}

\item{target_gene_expression}{Data frame of target-gene expression calls by
cell group; used to seed which cell types are analyzed when \code{cell_types}
is not supplied.}

\item{dact_results}{Differential abundance result table (one row per cell
type, e.g. from \code{dacts_when_abundant()}).}

\item{degs}{Differential expression table for the perturbation.}

\item{ref_expression}{Reference expression table.}

\item{ontologies}{Named list of ontology gene-set tables (columns
\code{gs_name}/\code{gene_short_name}) used for per-cell-type pathway enrichment.}

\item{combined_psg}{A \code{cell_state_graph} object or \code{igraph} used for
lineage traversal.}

\item{ai_notes_path}{Optional directory of precomputed per-cell-type
markdown background files, forwarded to \code{llm_fun}'s context; ignored when
\code{llm_fun} is \code{NULL}.}

\item{sig_p_val_thresh}{Significance cutoff used for DEG/pathway calls.}

\item{genes_of_interest}{Optional character vector used to highlight DEGs
(e.g. a regulator/TF panel) when building the genes-of-interest line.}

\item{empirical_p_thresh}{Optional artifact-aware significance cutoff
(\code{empirical_p}) applied to the genes-of-interest foreground; \code{1} disables
it and falls back to nominal \code{sig_p_val_thresh}.}

\item{llm_fun}{Optional function used to generate a per-cell-type narrative
summary; see Details. Defaults to \code{NULL}, which builds the impact table
without calling an LLM.}

\item{max_lineage_depth}{Optional maximum graph depth from roots for
included cell types.}

\item{cell_types}{Optional explicit cell types to seed lineage traversal,
instead of inferring them from \code{target_gene_expression}.}

\item{primary_impact_summary}{Optional text summary of a prior
(e.g. cell-autonomous) pass, forwarded to \code{llm_fun} as context.}

\item{excluded_cell_types}{Optional cell types to exclude from the result.}

\item{abundance_phenotypes}{Optional precomputed abundance phenotype table
(\code{cell_group}, \code{abundance_code}, \code{abundance_severity}).}

\item{fitness_phenotypes}{Optional precomputed fitness phenotype table
(\code{cell_group}, \code{fitness_label}, ...).}

\item{identity_phenotypes}{Optional precomputed identity phenotype table
(\code{cell_group}, \code{identity_label}, ...).}

\item{dact_expectations}{Optional per-cell-type literature-expectation table,
forwarded to \code{llm_fun} as context; ignored when \code{llm_fun} is \code{NULL}.}

\item{effect_type}{Optional label (e.g. \code{"Cell-autonomous"}) recorded for
this pass and forwarded to \code{llm_fun} as context.}

\item{pre_cited_gene_claims}{Optional pre-cited gene-background text
forwarded to \code{llm_fun}; ignored when \code{llm_fun} is \code{NULL}.}

\item{verbose}{Logical; if \code{TRUE}, prints debug messages instead of a
progress bar.}

\item{sig_pathways_by_cell_type}{Optional named list of precomputed
marker-specificity GSEA results, keyed by cell type.}

\item{sig_pathways_path}{Optional directory of precomputed
\code{sig_pathways.rds} files, one per cell type.}

\item{drop_llm_columns}{If \code{TRUE}, drop the LLM-derived columns
(\code{llm_summary}, \code{lineage_context_summary}, \code{llm_disrupted_pathways},
\code{llm_other_dysregulated_genes}) from the result. Forced to \code{TRUE}
whenever \code{llm_fun} is \code{NULL}, regardless of what's passed here, since
those columns are always empty in that case.}

\item{...}{Additional arguments forwarded to \code{llm_fun}.}
}
\value{
A tibble with one row per cell type, including \code{abundance_code},
\code{abundance_severity}, \code{identity_label}, \code{fitness_label}, per-cell-type
DEGs and pathway enrichment, and — when \code{llm_fun} is supplied and
\code{drop_llm_columns} is left \code{FALSE} — the LLM narrative columns.
}
\description{
Walks every cell type reachable from the perturbation's target-gene-expressing
states, joins in your abundance/fitness/identity phenotype calls and
significant DEGs, and runs pathway enrichment (\code{fgsea::fora()}) per cell type
to build a single impact table — one row per cell type — ready for
\code{impact_to_phenos()}/\code{plot_phenotypes_from_impact()}.
}
\details{
An optional \code{llm_fun} can be supplied to additionally have an LLM write a
short mechanistic summary for each cell type with a phenotype, genes of
interest, or pathway enrichment of its own (\code{llm_summary},
\code{llm_disrupted_pathways}, \code{llm_other_dysregulated_genes}). Leaving \code{llm_fun}
at its default of \code{NULL} skips that step entirely — no LLM server, Python, or
\code{reticulate} setup required — and returns the same tabular impact calls
(abundance/identity/fitness plus deterministic pathway enrichment) without
any generated prose. In that case the always-empty LLM columns are dropped
from the result automatically (\code{drop_llm_columns} is forced to \code{TRUE}); it
only needs to be set explicitly to drop those columns while still
supplying an \code{llm_fun}.
}
