% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assembly_utils.R
\name{abundances_over_interval}
\alias{abundances_over_interval}
\title{Predict abundances over an interval, marginal by default}
\usage{
abundances_over_interval(
  ccm,
  start,
  stop,
  interval_col = "timepoint",
  interval_step = 2,
  newdata = tibble(),
  marginalize_over = "auto",
  ...
)
}
\arguments{
\item{ccm, start, stop, interval_col, interval_step, newdata}{As in
\code{hooke::estimate_abundances_over_interval()}.}

\item{marginalize_over}{\code{"auto"} (default), a column name, or \code{NULL}. See
\code{\link[=resolve_marginalize_over]{resolve_marginalize_over()}}.}

\item{...}{Passed through to whichever estimator is used (e.g. \code{min_log_abund}).}
}
\description{
Thin dispatcher used everywhere platt reads a kinetic curve off a fitted model. It resolves
\code{marginalize_over} with \code{\link[=resolve_marginalize_over]{resolve_marginalize_over()}} and then calls either
\code{\link[=estimate_abundances_marginal]{estimate_abundances_marginal()}} (a nuisance factor exists; the default since portal issue #53) or
\code{hooke::estimate_abundances_over_interval()} (no such factor, or an explicit \code{NULL}). Having one
entry point is the point: a drawn curve and the table exported beside it cannot end up on different
read-outs.
}
