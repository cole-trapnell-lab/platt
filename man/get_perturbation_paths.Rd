% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_assembly.R
\name{get_perturbation_paths}
\alias{get_perturbation_paths}
\title{Find and Score Paths Between Loss/Gain Cell Groups for a Perturbation}
\usage{
get_perturbation_paths(
  perturbation_ccm,
  perturb_summary_tbl,
  pathfinding_graph,
  delta_log_abund_loss_thresh = 0
)
}
\arguments{
\item{perturbation_ccm}{A fitted genotype CCM (see \code{fit_genotype_ccm()}),
passed through to \code{score_paths_for_perturbations()}.}

\item{perturb_summary_tbl}{Per-cell-group loss/gain summary for this
perturbation; must have \code{cell_group}, \code{loss_when_present}, and
\code{gain_when_present} columns.}

\item{pathfinding_graph}{An \code{igraph} used for path search.}

\item{delta_log_abund_loss_thresh}{Minimum magnitude of
\code{loss_when_present}/\code{gain_when_present} for a cell group to count as lost
or gained (default \code{0}).}
}
\value{
A tibble of scored paths between gained/parentless-lost cell groups
and lost cell groups (one row per \code{from}/\code{to} pair with a path), or \code{NA}
if no lost cell groups are present, no paths are found, or path search
fails.
}
\description{
Given a fitted genotype CCM and its per-cell-group loss/gain summary, finds
shortest paths on \code{pathfinding_graph} from cell groups that are gained (or
lost with no lost parent) to cell groups that are lost, and scores each path
for how well it explains the perturbation's abundance changes.
}
