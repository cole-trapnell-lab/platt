% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phenotype_assignment_utils.R
\name{assign_phenotypes}
\alias{assign_phenotypes}
\title{Assign abundance, fitness, and identity phenotype labels for every perturbation}
\usage{
assign_phenotypes(
  contrast_tbls,
  fitness_gene_sets,
  identity_gene_sets,
  combined_psg,
  cell_type_denylist = NULL,
  num_threads = NULL,
  use_summarized_tbl = TRUE,
  minSize = 10,
  maxSize = 5000,
  nperm = 1000,
  abundance_q_cut = 0.1
)
}
\arguments{
\item{contrast_tbls}{A tibble with one row per perturbation, carrying the
columns \code{perturb_name}, \code{perturb_group}, \code{run}, the list-columns
\code{perturb_time_window} and \code{differential_expression_filename}, and the
differential cell abundance list-column selected by \code{use_summarized_tbl}.}

\item{fitness_gene_sets}{Named list of gene sets used for the fitness fgsea.}

\item{identity_gene_sets}{Per-cell-type identity gene sets, as returned by
\code{\link[=construct_identity_gene_sets]{construct_identity_gene_sets()}}.}

\item{combined_psg}{Cell state graph supplying the lineage relationships
(parents, descendants, alternative fates) used for identity labelling.}

\item{cell_type_denylist}{Character vector of cell types to drop before
classification, or \code{NULL} to keep all of them.}

\item{num_threads}{Number of workers. \code{NULL} or a non-positive value runs
serially.}

\item{use_summarized_tbl}{If \code{TRUE}, read the \code{summarized_differential_cell_abundance}
column. If \code{FALSE}, derive the equivalent columns from
\code{differential_cell_abundance} via \code{\link[=dacts_when_abundant]{dacts_when_abundant()}}.}

\item{minSize, maxSize}{Gene set size bounds passed to fgsea.}

\item{nperm}{Permutation count passed to fgsea.}

\item{abundance_q_cut}{q-value cutoff for the abundance codes, passed to
\code{\link[=assign_abundance_code]{assign_abundance_code()}}. Defaults to \code{0.1}; pass \code{0.01} to reproduce the
stricter \verb{A3 Near-loss} gate used before this was configurable. Note that
\code{\link[=assign_abundance_severity]{assign_abundance_severity()}} keeps its own graded thresholds and is not
affected by this argument.}
}
\value{
A tibble with one row per perturbation and cell type, containing
\code{cell_group}, the perturbation identifiers, the time window bounds,
\code{abundance_code}, \code{abundance_severity}, and the \code{fitness_labels},
\code{fitness_fgsea}, \code{identity_labels} and \code{identity_fgsea} list-columns.
}
\description{
Iterates over the perturbations in \code{contrast_tbls}, loads each one's DEG
table, and classifies every cell type along three axes: abundance (\verb{A*}
codes from the differential cell abundance test), fitness, and identity
(both from fgsea over the supplied gene sets).
}
\keyword{internal}
