% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deg.R
\name{calc_graph_overrep_genes}
\alias{calc_graph_overrep_genes}
\title{Calculate Graph Overrepresented Genes}
\usage{
calc_graph_overrep_genes(
  ref_degs,
  pathway_type = c("GO:BP", "GO:MF"),
  species = "Danio rerio"
)
}
\arguments{
\item{ref_degs}{A data frame containing reference differentially expressed genes (DEGs).
It should include columns such as \code{cell_state}, \code{gene_short_name}, \code{gene_id},
and \code{interpretation}. If the data frame contains a \code{gene_class_scores} column,
it will be unnested.}

\item{pathway_type}{A character vector specifying the type of pathways to analyze.
Options are \code{"GO:BP"} (Biological Process) or \code{"GO:MF"} (Molecular Function).
Defaults to \code{c("GO:BP", "GO:MF")}.}

\item{species}{A character string specifying the species for pathway analysis.
Defaults to \code{"Danio rerio"}.}
}
\value{
A nested data frame where each row corresponds to a cell state, and the
\code{overrep_pathways} column contains the overrepresented pathways for that cell state.
}
\description{
This function calculates overrepresented pathways for genes in a given dataset,
grouped by cell state. It processes the input data, simplifies gene interpretation,
and computes overrepresented pathways for each cell state.
}
\details{
The function simplifies the \code{interpretation} column into categories
(\code{"Up"}, \code{"Down"}, \code{"Maintained"}, or retains the original value if none match).
It then groups the data by \code{cell_state} and calculates overrepresented pathways
using the \code{calc_overrep_genes} function.
}
\examples{
# Example usage:
ref_degs <- data.frame(
  cell_state = c("State1", "State2"),
  gene_short_name = c("GeneA", "GeneB"),
  gene_id = c("ID1", "ID2"),
  interpretation = c("Upregulated", "Downregulated")
)
result <- calc_graph_overrep_genes(ref_degs, pathway_type = "GO:BP", species = "Danio rerio")

}
