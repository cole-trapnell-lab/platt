% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assembly_utils.R
\name{resolve_marginalize_over}
\alias{resolve_marginalize_over}
\title{Resolve which factor a marginal read-out should average over}
\usage{
resolve_marginalize_over(ccm, interval_col, arg = "auto")
}
\arguments{
\item{ccm}{A \code{cell_count_model}.}

\item{interval_col}{The interval variable, excluded from the candidates.}

\item{arg}{The user's \code{marginalize_over} argument.}
}
\value{
A column name, or \code{NULL}.
}
\description{
\code{"auto"} resolves to the fitted full model's own nuisance factor: the single term with recorded
factor levels that is not \code{interval_col} (in practice the collection/batch column). It returns
\code{NULL} when the model has no such term, so a caller that dispatches on the result falls back to a
plain prediction -- a model without a nuisance factor has nothing to marginalise and is not affected
by portal issue #53. Anything else, including \code{NULL}, is passed through unchanged, so an explicit
column name still forces that factor and an explicit \code{NULL} still selects the pre-2026-09 read-out.
}
