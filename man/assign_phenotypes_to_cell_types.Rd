% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phenotype_assignment_utils.R
\name{assign_phenotypes_to_cell_types}
\alias{assign_phenotypes_to_cell_types}
\title{Assign phenotype labels for every cell type within one perturbation}
\usage{
assign_phenotypes_to_cell_types(
  dact_tbl,
  deg_tbl,
  gene_sets,
  identity_gene_sets,
  combined_psg,
  perturb_name,
  perturb_group,
  perturb_time_window,
  run,
  pb = NULL,
  log_fn = NULL,
  num_threads = NULL,
  minSize = 10,
  maxSize = 5000,
  nperm = 1000,
  abundance_q_cut = 0.1
)
}
\arguments{
\item{dact_tbl}{Differential cell abundance table for this perturbation,
carrying \code{cell_group}, \code{change_when_present} and
\code{change_when_present_q_val}.}

\item{deg_tbl}{Differential expression table for this perturbation.}

\item{gene_sets}{Named list of gene sets used for the fitness fgsea.}

\item{identity_gene_sets}{Per-cell-type identity gene sets, as returned by
\code{\link[=construct_identity_gene_sets]{construct_identity_gene_sets()}}.}

\item{combined_psg}{Cell state graph supplying lineage relationships.}

\item{perturb_name, perturb_group, run}{Identifiers copied onto every output
row.}

\item{perturb_time_window}{A list or data frame with \code{start_time} and
\code{stop_time}, recorded as the output's time window bounds.}

\item{pb}{Optional progress bar object; ticked once per cell type.}

\item{log_fn}{Optional logging function called with progress messages.}

\item{num_threads}{Number of workers. \code{NULL} or a non-positive value runs
serially.}

\item{minSize, maxSize}{Gene set size bounds passed to fgsea.}

\item{nperm}{Permutation count passed to fgsea.}

\item{abundance_q_cut}{q-value cutoff for the abundance codes, passed to
\code{\link[=assign_abundance_code]{assign_abundance_code()}}. Defaults to \code{0.1}. Does not affect
\code{\link[=assign_abundance_severity]{assign_abundance_severity()}}, which keeps its own graded thresholds.}
}
\value{
A tibble with one row per cell type; see \code{\link[=assign_phenotypes]{assign_phenotypes()}} for
the columns.
}
\description{
Worker behind \code{\link[=assign_phenotypes]{assign_phenotypes()}}; classifies each cell type in
\code{dact_tbl} along the abundance, fitness, and identity axes.
}
\keyword{internal}
