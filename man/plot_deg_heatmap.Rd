% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_degs.R
\name{plot_deg_heatmap}
\alias{plot_deg_heatmap}
\title{Plot a Clustered Heatmap for a DEG Gene List}
\usage{
plot_deg_heatmap(
  deg_table,
  genes,
  cell_groups = NULL,
  sig_p_value_thresh = 0.05,
  min_log_mean_expression = -2,
  gene_col = "id",
  gene_label_col = "gene_short_name",
  cell_group_col = "cell_group",
  lfc_col = "perturb_to_ctrl_shrunken_lfc",
  p_value_col = "perturb_to_ctrl_p_value",
  present_col = "present_above_thresh",
  log_mean_expression_col = "log_mean_expression"
)
}
\arguments{
\item{deg_table}{A data frame containing DEG results. By default it must
include gene identifiers, gene labels, cell-group labels, shrunken log fold
changes, p-values, a detection flag, and log mean expression values.}

\item{genes}{Character vector of genes or IDs to include in the heatmap.
Matching is performed against \code{gene_col}.}

\item{cell_groups}{Optional character vector of cell groups to include. If
\code{NULL}, cell groups are selected automatically by requiring at least one
gene from \code{genes} to satisfy the detection, significance, and mean
expression thresholds.}

\item{sig_p_value_thresh}{Numeric significance threshold applied to
\code{p_value_col} when selecting cell groups and when outlining significant
tiles in the plot.}

\item{min_log_mean_expression}{Numeric minimum value in
\code{log_mean_expression_col} required for a DEG to count toward retaining a
cell group.}

\item{gene_col}{Name of the column containing the gene identifiers used to
match \code{genes}.}

\item{gene_label_col}{Name of the column used for y-axis labels in the
heatmap.}

\item{cell_group_col}{Name of the column containing cell-group labels.}

\item{lfc_col}{Name of the column containing log fold-change values used for
tile fill.}

\item{p_value_col}{Name of the column containing DEG p-values.}

\item{present_col}{Name of the logical column indicating whether the gene is
detected above the expression threshold for that cell group.}

\item{log_mean_expression_col}{Name of the column containing mean expression
values on a log scale.}
}
\value{
A \code{ggplot} object showing clustered log fold changes for the selected
genes across retained cell groups.
}
\description{
Build a heatmap from a DEG table for a supplied set of genes, typically a
set of common DEGs shared across perturbations or cell groups. Cell groups
are retained only if they contain at least one gene from \code{genes} that is
detected, significant, and above the specified mean-expression threshold.
The retained gene-by-cell-group log fold-change matrix is then hierarchically
clustered along both axes and plotted with significant tiles outlined.
}
\examples{
\dontrun{
plot_deg_heatmap(
    deg_table = degs,
    genes = common_degs,
    cell_groups = c("Neurons", "Glia"),
    sig_p_value_thresh = 0.05,
    min_log_mean_expression = -2
)
}
}
