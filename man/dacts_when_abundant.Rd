% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phenotype_assignment_utils.R
\name{dacts_when_abundant}
\alias{dacts_when_abundant}
\title{Pick one abundance contrast per cell type, where the cell type was abundant}
\usage{
dacts_when_abundant(
  differential_cell_abundance,
  percent_max_thresh = 0.1,
  with_ties = FALSE
)
}
\arguments{
\item{differential_cell_abundance}{A tibble of abundance contrasts carrying
\code{cell_group}, \code{timepoint_x}, \code{delta_log_abund}, \code{delta_q_value},
\code{percent_max_abund} and \code{present_above_thresh}. The latter two are added
downstream of Hooke by the wild-type reference join, so a raw
\code{compare_abundances()} result will not have them.}

\item{percent_max_thresh}{Rows below this fraction of the cell type's peak
abundance are neutralised rather than dropped: \code{delta_log_abund} is set to
\code{0} and \code{delta_q_value} to \code{1}. Defaults to \code{0.10}. Pass \code{0} to neutralise
nothing.}

\item{with_ties}{Passed to \code{\link[dplyr:slice]{dplyr::slice_min()}}. \code{FALSE} (the default) keeps
exactly one row per cell type even when several tie on \code{delta_q_value}.}
}
\value{
A tibble with at most one row per \code{cell_group}. Cell types with no
qualifying row are absent, not \code{NA} -- see the section above.
}
\description{
Reduces a decorated Hooke abundance contrast table to one row per cell type:
the most significant timepoint among those where the cell type was actually
present in the wild-type reference.
}
\details{
This is the canonical implementation. It was duplicated verbatim in
zscapetools for some time; that copy now calls this one. Do not reintroduce a
second copy -- one of the two was exported and the other was not, so bare
calls resolved by \code{library()} order.
}
\section{Rows are dropped, not flagged}{

Cell types are \strong{removed entirely} when every one of their rows has
\code{present_above_thresh} \code{FALSE} or \code{NA}, or a missing \code{delta_log_abund}. That
matters more than it looks: \code{present_above_thresh} is a property of the
WILD-TYPE reference, set in mcclintock's \code{fit_wt_model.R} as
\code{percent_max_abund >= 0.1}, so it marks states the experiment's timepoint
window could not assess -- not states the perturbation removed.

On a 15-perturbation GENE6 run, 585 of 5,203 cell-type/perturbation pairs
(11.2\%) were dropped this way, every one of them for \code{present_above_thresh}
and none for a missing \code{delta_log_abund}.

Callers that build a per-cell-type table from this output therefore lose
those cell types silently. To report them instead, capture the cell-type
universe \emph{before} calling this and pass it to
\code{\link[=assign_phenotypes_to_cell_types]{assign_phenotypes_to_cell_types()}} as \code{all_cell_types}, which surfaces them
as \code{"AN Not assessed"}.
}

\examples{
\dontrun{
# one row per cell type, neutralising nothing
dacts <- dacts_when_abundant(differential_cell_abundance, percent_max_thresh = 0)
}
}
