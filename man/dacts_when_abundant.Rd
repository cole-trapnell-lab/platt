% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phenotype_assignment_utils.R
\name{dacts_when_abundant}
\alias{dacts_when_abundant}
\title{Pick the Peak-Effect Timepoint per Cell Type from a Differential Abundance Table}
\usage{
dacts_when_abundant(
  differential_cell_abundance,
  percent_max_thresh = 0.1,
  with_ties = FALSE
)
}
\arguments{
\item{differential_cell_abundance}{A per-timepoint differential abundance
table. Must have \code{cell_group}, \code{timepoint_x}, \code{delta_log_abund},
\code{delta_q_value}, \code{percent_max_abund}, and \code{present_above_thresh}.}

\item{percent_max_thresh}{Minimum \code{percent_max_abund} (relative to the cell
type's peak abundance) for a timepoint to be treated as having a real
abundance change; below this, \code{delta_log_abund} is zeroed out and
\code{delta_q_value} is forced to \code{1} before picking the peak (default \code{0.10}).}

\item{with_ties}{Passed to \code{dplyr::slice_min()}; whether to keep all rows
tied for the minimum \code{delta_q_value} per cell type (default \code{FALSE},
i.e. one row per cell type).}
}
\value{
\code{differential_cell_abundance}, filtered to rows with a non-\code{NA}
\code{delta_log_abund} and \code{present_above_thresh == TRUE}, then reduced to one
row per \code{cell_group} — the timepoint with the smallest \code{delta_q_value}
after the \code{percent_max_thresh} gate.
}
\description{
Collapses a per-timepoint differential-abundance table down to one row per
cell type: the timepoint with the strongest (most significant) abundance
change, restricted to timepoints where the cell type is reasonably abundant.
This is the usual way to build the \code{dact_results} input expected by
\code{summarize_impact_in_lineage_context()}/\code{summarize_impact_in_lineage_context_no_llm()}
from a raw per-timepoint \code{differential_cell_abundance} table.
}
\keyword{internal}
