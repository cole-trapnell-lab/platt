% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/narrative.R
\name{summarize_cell_type_impact}
\alias{summarize_cell_type_impact}
\title{Summarize a Single Cell Type's Perturbation Impact}
\usage{
summarize_cell_type_impact(
  ct,
  perturbation_description = NULL,
  target_gene_expression,
  dact_results,
  degs,
  ref_expression,
  ontologies,
  ai_notes_path = NULL,
  sig_p_val_thresh = 0.05,
  genes_of_interest = NULL,
  empirical_p_thresh = 1,
  power_thresh = 0.8,
  top_n_pathways = 5,
  abundance_phenotypes = NULL,
  fitness_phenotypes = NULL,
  identity_phenotypes = NULL,
  dact_expectations = NULL,
  effect_type = NULL,
  sig_pathways_by_cell_type = NULL,
  sig_pathways_path = NULL
)
}
\arguments{
\item{ct}{The cell type to summarize.}

\item{perturbation_description}{Optional free-text description of the
perturbation, included in \code{summary}.}

\item{target_gene_expression}{Data frame of target-gene expression calls by
cell group; used to report which perturbed genes are expressed here.}

\item{dact_results}{Differential abundance result table (one row per cell
type); used as a fallback abundance call when \code{abundance_phenotypes} has
no row for \code{ct}.}

\item{degs}{Differential expression table for the perturbation.}

\item{ref_expression}{Reference expression table (accepted for interface
compatibility; not used directly by this function).}

\item{ontologies}{Named list of ontology gene-set tables (\code{gs_name}/
\code{gene_short_name} columns) used for pathway enrichment.}

\item{ai_notes_path}{Optional directory of precomputed per-cell-type
markdown background/pathway files; when supplied (and
\code{sig_pathways_by_cell_type}/\code{sig_pathways_path} are not), pathway
enrichment is restricted to pathways an ancestor cell type already
flagged as significant.}

\item{sig_p_val_thresh}{Significance cutoff used for DEG and pathway calls.}

\item{genes_of_interest}{Optional character vector used to build the
genes-of-interest line (e.g. a regulator/TF panel).}

\item{empirical_p_thresh}{Optional artifact-aware significance cutoff
(\code{empirical_p}) applied to the DEG foreground; \code{1} disables it and falls
back to nominal \code{sig_p_val_thresh}.}

\item{power_thresh}{Power threshold for classifying a non-significant
\code{dact_results} row as \code{"no_change"} vs \code{"unknown (underpowered)"}.}

\item{top_n_pathways}{Maximum enriched pathways included per ontology in
\code{summary}'s pathway lines.}

\item{abundance_phenotypes}{Optional precomputed abundance phenotype table
(\code{cell_group}, \code{abundance_code}, \code{abundance_severity}); takes precedence
over \code{dact_results} for this cell type when present.}

\item{fitness_phenotypes}{Optional precomputed fitness phenotype table
(\code{cell_group}, \code{fitness_label}, ...).}

\item{identity_phenotypes}{Optional precomputed identity phenotype table
(\code{cell_group}, \code{identity_label}, ...).}

\item{dact_expectations}{Optional per-cell-type literature-expectation
table; surfaced in \code{summary} as a cited "Literature expectation" line.}

\item{effect_type}{Optional label (e.g. \code{"Cell-autonomous"}), included in
\code{summary}.}

\item{sig_pathways_by_cell_type}{Optional named list of precomputed
marker-specificity GSEA results, keyed by cell type; checked before
\code{sig_pathways_path} and \code{ai_notes_path}.}

\item{sig_pathways_path}{Optional directory of precomputed
\code{sig_pathways.rds} files, one per cell type; checked before
\code{ai_notes_path}.}
}
\value{
A named list with (among others) \code{summary} (the plain-text block),
\code{abundance_code}, \code{abundance_severity}, \code{fitness_label},
\code{identity_label}, \code{present_above_thresh}, \code{degs} (this cell type's
filtered DEG table), \code{goi_line}, \code{has_regulator_goi}, and \code{pathways}
(FDR-significant, non-empty-overlap enrichment results).
}
\description{
The per-cell-type worker behind \code{summarize_impact_in_lineage_context()}:
looks up this cell type's abundance/fitness/identity phenotype calls,
filters its significant DEGs (optionally gated on \code{empirical_p}), runs
pathway enrichment (\code{fgsea::fora()}), and assembles a plain-text summary
block (\code{summary}) that either becomes the impact-table row directly (when
called with \code{llm_fun = NULL}) or is handed to an LLM as \code{cell_impact_text}
context for that cell type.
}
\keyword{internal}
