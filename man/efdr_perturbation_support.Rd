% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/empirical_fdr_model.R
\name{efdr_perturbation_support}
\alias{efdr_perturbation_support}
\title{Per-cell-type perturbation-arm replicate support.}
\usage{
efdr_perturbation_support(
  cds,
  sample_group = "embryo_ID",
  cell_group = "cell_type",
  control_ids = c("ctrl-inj"),
  perturbation_col = "perturbation"
)
}
\arguments{
\item{cds}{contrast cell_data_set.}

\item{sample_group, cell_group, control_ids, perturbation_col}{colData columns/values.}

\item{min_cells}{Minimum cells for an embryo to count as a replicate. Default 1:
the replicate unit is the embryo (an embryo either has the cell type or not);
cells-per-embryo feeds the per-pseudobulk variance, not the replicate count, and
a higher bar would wrongly drop cell types that are present across many embryos
but sparse per embryo (common in shallow captures). Matches the within-state DEG,
which fits on any embryo with cells (\code{min_cells_per_pseudobulk = NULL}).}
}
\value{
tibble(cell_group, n_pert_pb, n_ctrl_pb, n_eff_pb, n_pert_cells, n_ctrl_cells).
}
\description{
Counts, per cell type, the number of \strong{perturbation} embryos (pseudobulks)
contributing at least \code{min_cells} cells to the within-state contrast. This is
the replicate count the pseudobulk GLM actually has on the perturbation side,
and it is the honest degrees-of-freedom for the contrast: when a cell type is
depleted in the perturbation, the deep control arm supplies a small standard
error as if the contrast were well-powered, inflating |z| and manufacturing
spurious (predominantly down) calls. \code{\link[=annotate_empirical_fdr_model]{annotate_empirical_fdr_model()}} uses this
to floor the ordinary p by a t-distribution with \code{n_pert_pb - 1} df, and to
gate cell types with <2 perturbation pseudobulks (no valid two-group contrast).
}
